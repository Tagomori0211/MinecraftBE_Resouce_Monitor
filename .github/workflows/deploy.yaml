name: Deploy Minecraft Dashboard (Hybrid On-Premise)

on:
  push:
    branches:
      - main
  repository_dispatch:
    types: [new-minecraft-backup]

jobs:
  deploy_on_premise:
    runs-on: [self-hosted, Linux] 
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    # --- 既存のDockerデプロイ ---
    - name: Create .env file
      run: |
        echo "POSTGRES_USER=${{ secrets.POSTGRES_USER }}" >> .env
        echo "POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}" >> .env
        echo "POSTGRES_DB=${{ secrets.POSTGRES_DB }}" >> .env
        echo "FLASK_APP_SECRET_KEY=${{ secrets.FLASK_APP_SECRET_KEY }}" >> .env

    - name: Build and Deploy Dashboard (Docker)
      run: |
        docker compose up -d --build --remove-orphans
        docker image prune -f

    # --- 【追加】K3sへの自動デプロイ ---
    # DevOps VMのRunnerから、MCBE VMのK3sを操作する
    - name: Deploy to K3s (Minecraft Server)
      env:
        # GitHub Secretsに登録した kubeconfig をファイルに書き出す
        KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG_DATA }}
      run: |
        # .kubeディレクトリを作成
        mkdir -p ~/.kube
        
        # Base64エンコードされた設定をデコードして保存
        echo "$KUBE_CONFIG_DATA" | base64 -d > ~/.kube/config
        
        # 権限を厳しくする（kubectlが怒らないように）
        chmod 600 ~/.kube/config
        
        # cAdvisorのマニフェストを適用！
        echo "🚀 Applying cAdvisor manifest to K3s..."
        kubectl apply -f MCBEs/cadvisor.yaml
        
        # 設定変更を確実にするためPodを再作成
        kubectl delete pods -l app=cadvisor --ignore-not-found=true